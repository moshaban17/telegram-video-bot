name: Video Processor

on:
  push:
    paths:
      - '.github/workflows/video_processor.yml'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test workflow
        run: echo "Video Processor workflow is registered correctly"
